import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#01112D",
    },
    secondary: {
      main: "#CC8F1F",
      light: "#1f7b80",
    },
  },
});
export default theme;
